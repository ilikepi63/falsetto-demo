import Schema from "./schema";
import Table from "./table";
import ClusteringColumn from "./clustering-column";
/** Function to retrieve the first-most query that
 * can support the schema.
 *
 * @param keys - keys the query uses.
 * @param schema - the schema given
 */
export declare const getTableFromSchema: (exactKeys: Array<string>, rangeKeys: Array<string>, schema: Schema) => Table;
/** For a query to be a viable solution to the keys, they must match the partition
 *
 * @param schema
 * @param keys
 */
export declare const partitionDoesMatch: (keys: Array<string>) => (table: Table) => boolean;
export declare const getPoint: (keys: Array<string>) => (col: ClusteringColumn) => number;
export declare const getBestFitTable: (tables: Array<Table>, rangeKeys: Array<string>) => Table;
